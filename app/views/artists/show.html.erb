

  <h1>  <%= @artist.name %> </h1>
  <ul>
    <% @artist.songs.each do |s| %>
      <li>  <%= link_to s.title, song_path(s) %> </li>
      <% end %>

 <%= link_to '<button type = "button">Edit</button>'.html_safe, edit_artist_path %>

</ul>
